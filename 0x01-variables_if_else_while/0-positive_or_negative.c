#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - assigns a random number and executes it
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	srand(time));
	n = rand() - RAND_MAX / 2;
	printf("n is :  n > 0; n = 0; n < 0/n");
	n > 0 is positive;
	n = 0 is zero;
	n < 0 is negative;
return (0);
}
