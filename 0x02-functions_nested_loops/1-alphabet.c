#include main.h

/**
 * main - Print alphabet in lowercase.
 *
 * Return: Always 0
 */

int main(void)
{
	void print_alphabet(void);

	char letter;

	for (letter = 'a'; letter <= 'z')
		letter++;
		_putchar(letter);

	putchar('\n')

	return (0);
}
